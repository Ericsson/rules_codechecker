--- a/BUILD.bazel
+++ b/BUILD.bazel
@@ -10,5 +10,6 @@
     visibility = ["//visibility:public"],
     includes = ["include"],
     hdrs = glob(["include/**/*.h"]),
+    # Newer compilers throw warnings without this.
+    copts = ["-include cstdint"],
     srcs = glob(["src/**/*.cpp", "src/**/*.h"]),
 )
